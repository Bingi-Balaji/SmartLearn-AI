{
  "name": "ai-ml-adaptive-learning-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm --prefix frontend start",
    "dev": "npm --prefix frontend start",
    "build": "npm --prefix frontend run build"
  }
}
